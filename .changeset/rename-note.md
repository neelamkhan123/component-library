---
"neelam-ui": patch
---

Note the rename from `@neelamkhan21/ui` in the README, with the import and Tailwind `@source` changes shown as diffs. npm only refreshes a package's README when a new version is published, so this needs a release to become visible on the package page.
